"""CrewHub Security Triage Agent — a LangGraph agent declared to siem-soar.

This package IS the agent: a LangGraph StateGraph that proves its own agency
by running ``prove_agency`` as a graph node and emitting ``manifest.json`` —
the declaration siem-soar's discover-agents pipeline consumes.
"""
__version__ = "2.0.0"
